name: Auto discover recruitment jobs

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "扫描模式"
        required: true
        default: "full"
        type: choice
        options:
          - full
          - recent
      company:
        description: "只扫描某一家企业（可留空）"
        required: false
        type: string
  push:
    paths:
      - "crawler/**"
      - ".github/workflows/auto-discover.yml"
  schedule:
    - cron: "17 */4 * * *"

permissions:
  contents: write

concurrency:
  group: auto-discover-recruitment
  cancel-in-progress: true

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install crawler dependencies
        run: pip install -r crawler/requirements.txt

      - name: Discover jobs
        env:
          BRAVE_SEARCH_API_KEY: ${{ secrets.BRAVE_SEARCH_API_KEY }}
          MANUAL_SCAN_MODE: ${{ github.event.inputs.mode }}
          MANUAL_COMPANY: ${{ github.event.inputs.company }}
        run: python crawler/crawl_jobs.py

      - name: Commit discovered jobs
        run: |
          if [ -z "$(git status --porcelain -- data/auto-jobs.json auto-jobs-data.js crawler/state.json)" ]; then
            echo "No changes."
            exit 0
          fi

          mkdir -p /tmp/unijob-scan/crawler /tmp/unijob-scan/data
          cp data/auto-jobs.json /tmp/unijob-scan/data/auto-jobs.json
          cp auto-jobs-data.js /tmp/unijob-scan/auto-jobs-data.js
          cp crawler/state.json /tmp/unijob-scan/crawler/state.json

          git config user.name "job-tracker-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --hard origin/main

          cp /tmp/unijob-scan/data/auto-jobs.json data/auto-jobs.json
          cp /tmp/unijob-scan/auto-jobs-data.js auto-jobs-data.js
          cp /tmp/unijob-scan/crawler/state.json crawler/state.json

          git add data/auto-jobs.json auto-jobs-data.js crawler/state.json
          if git diff --cached --quiet; then
            echo "Latest main already contains the same scan result."
            exit 0
          fi
          git commit -m "data: auto-discover recruitment updates"
          git push origin HEAD:main
